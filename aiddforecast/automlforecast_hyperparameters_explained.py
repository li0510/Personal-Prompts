# automlforecast_hyperparameters_explained.py
"""
AutoMLForecast 框架超参数业务可解释性文档
==========================================

本文档对框架内 AutoLightGBM 模型的全部业务可配置超参数
进行业务层面的翻译解析。

面向读者：
    - 供应链计划部门 / 框架用户：通过"一问"理解每个参数在业务上控制什么
    - 财务 / 销售 / 营销部门：通过"二问"理解参数对自己的影响
    - 技术人员：通过"三问"掌握调整方法和验证指标

模板结构（三问）：
    一问 — 业务上控制什么？调大/调小对库存、资金、缺货风险的影响
    二问 — 对财务和销售/营销部门分别意味着什么？何时调整？
    三问 — 实际效果验证：效果好如何保持？不好如何调整？

预设值来源：
    - 沃尔玛（零售业标杆）：SKU 海量，促销频繁，需抵抗偶发大单冲击
    - 麦当劳（餐饮业标杆）：SKU 集中，缺货代价极高，需求模式相对稳定
"""

# ═══════════════════════════════════════════════════════════════════
# AutoLightGBM 超参数业务可解释性
# ═══════════════════════════════════════════════════════════════════

LIGHTGBM_PARAMS = """
## AutoLightGBM 业务可配置超参数

### 1. `n_estimators` — 树的数量

**一问：业务上控制什么？调大/调小对库存、资金、缺货风险分别意味着什么？**

| 维度 | 说明 |
|------|------|
| 技术含义 | 梯度提升中弱学习器（树）的总数 |
| 业务翻译 | "模型学习多久"——树越多学得越充分，但过犹不及 |

| 调大（如 1000） | 调小（如 100） |
|----------------|---------------|
| 训练慢，能捕捉复杂促销+季节叠加模式 | 训练快，但可能欠拟合，漏掉重要模式 |

| 对库存的影响 | 对资金的影响 | 对缺货风险的影响 |
|-------------|-------------|----------------|
| 调大：预测更精准，库存水位更合理 | 调大：资金占用优化，减少冗余库存 | 调大：缺货风险降低，促销季不断货 |
| 调小：预测偏差可能大，库存波动 | 调小：资金效率不确定性高 | 调小：特殊场景可能缺货 |

**沃尔玛预设搜索范围 [20, 1000]**：海量 SKU，部分商品需求模式复杂（促销叠加季节性），需要足够的树捕捉。
**麦当劳预设搜索范围 [20, 500]**：需求模式相对稳定，较少树即可收敛，过多反而过拟合。

**二问：对财务和销售/营销部门分别意味着什么？什么时候该调？**

| 部门 | 影响 | 调整建议 |
|------|------|---------|
| 财务 | 树越多训练时间越长，算力成本越高 | 在预算范围内，优先保证模型质量 |
| 销售/营销 | 复杂促销模式需要足够树才能捕捉 | 如果促销季预测持续偏低，可能树不够 |

| 现象 | 调整方向 |
|------|---------|
| 促销季预测持续偏差，缺货频发 | 提高搜索上限 |
| 训练时间过长且准确度无提升 | 降低搜索上限 |
| 正常销售季预测准确 | **不动** |

**三问：实际效果验证**

| 验证指标 | 好的标准 | 不好时的调整 |
|---------|---------|------------|
| 促销季预测偏差率 | 偏差 < 10% | 偏差大 → 提高 n_estimators 上限 |
| 训练时长 | 在计划窗口内完成 | 超时 → 降低上限 |
| 日常季预测准确率 | 稳定在目标范围 | 不动 |

---

### 2. `learning_rate` — 学习率

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 每棵树对最终预测的贡献权重 | "模型学习新模式的激进程度"——快速适应还是保守微调 |

| 调大（如 0.1） | 调小（如 0.01） |
|---------------|----------------|
| 快速适应新需求模式，但可能对偶发大单反应过激 | 学习慢但稳定，不易被促销大单带偏 |

| 对库存/资金/缺货风险 | 影响 |
|---------------------|------|
| 调大：可能对突发大单过激反应 → 库存波动 | 缺货风险降低但库存成本上升 |
| 调小：预测平滑但可能漏掉新模式 | 库存平稳但缺货风险略高 |

**沃尔玛预设 0.05**：需抵抗促销冲击，不宜过大。
**麦当劳预设 0.05**：需求稳定，0.05 平衡收敛速度与稳定性。

**二问：对财务/销售影响**

| 场景 | 调整建议 |
|------|---------|
| 新品上线初期，需求模式不明确 | 可略调大到 0.08，快速适应 |
| 成熟品类，需求稳定 | 保持 0.05 或降到 0.03 |
| 刚经历过异常事件，不希望模型被带偏 | 调小到 0.02 |

**三问：效果验证**

| 验证指标 | 好的标准 | 不好时的调整 |
|---------|---------|------------|
| 预测稳定性 | 相邻周预测值不出现剧烈跳变 | 波动大 → 调小 learning_rate |
| 新品类适应速度 | 新品上线 4-6 周内预测趋于合理 | 持续偏低 → 略调大 |

---

### 3. `num_leaves` — 叶子节点数

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 每棵树最多有多少个叶子节点 | "每棵树的精细度"——叶子越多，切分越细，能捕获更细微的需求模式 |

| 调大（如 1024） | 调小（如 31） |
|----------------|--------------|
| 能描述复杂促销+季节叠加模式 | 更泛化，不易过拟合 |

| 对库存 | 对资金 | 对缺货风险 |
|--------|--------|-----------|
| 调大：可能过拟合历史促销，泛化差 | 库存波动大，资金占用不稳定 | 缺货风险不稳定 |
| 调小：预测平滑，但可能漏掉复杂叠加模式 | 库存稳定 | 特殊叠加场景可能缺货 |

**沃尔玛预设 [2, 4096]**：促销叠加季节性的复杂模式需要较宽搜索空间。
**麦当劳预设 [2, 1024]**：模式相对简单，过大叶子数容易过拟合。

**二问**：财务/销售一般不需要关注此参数，Optuna 自动搜索最佳值。

**三问**：若预测曲线频繁波动且无明显业务原因 → 降低上限。若叠加场景（促销+节假日）预测偏差大 → 提高上限。

---

### 4. `max_depth` — 最大深度

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 每棵树的最大深度 | "推理链条长度"——更深意味着更复杂的业务逻辑组合 |

| 调大（如 12） | 调小（如 4） |
|--------------|-------------|
| 能描述促销+天气+节假日+趋势多因素叠加 | 推理简单，泛化好 |

**沃尔玛预设 [3, 16]**：多因素叠加场景常见。
**麦当劳预设 [3, 10]**：叠加场景少，较浅即可。

**二问/三问**：Optuna 自动搜索。若过拟合明显 → 降低上限。若叠加场景预测不准 → 提高上限。

---

### 5. `lambda_l1` / `lambda_l2` — 正则化

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 对模型复杂度的惩罚力度 | "防过拟合的刹车力度"——越大越保守，越不容易被偶发大单带偏 |

| 调大 | 调小 |
|------|------|
| 预测更平滑，抵抗异常值 | 对特殊模式更敏感 |

**沃尔玛预设 [1e-8, 10.0]**：促销偶发大单需要适中正则化——不能太大（忽略促销），不能太小（被促销带偏）。
**麦当劳预设 [1e-8, 1.0]**：需求稳定，较小正则化即可。

**二问**：财务/销售一般不需要关注。

**三问**：若预测在促销季明显偏低（正则化过大忽略了促销）→ 降低上限。若预测在平稳期也频繁波动（过拟合噪音）→ 提高下限。

---

### 6. `min_data_in_leaf` — 叶子最小样本数

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 每个叶子节点最少需要多少条历史记录支撑 | "一个结论至少需要多少条历史记录支撑"——防过拟合的最后一道门槛 |

| 调大（如 100） | 调小（如 5） |
|---------------|-------------|
| 只有反复出现的模式才被学习 | 偶发模式也可能被学习 |

**沃尔玛预设 [5, 200]**：促销虽是偶发但真实，不宜设太大。
**麦当劳预设 [10, 200]**：下限略高，需求稳定不需要学习偶发噪音。

**二问/三问**：若促销季预测正常但平稳期波动大 → 提高下限。若促销季完全捕捉不到 → 降低下限。

---

### 7. `feature_fraction` — 特征采样比例

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 每次训练迭代随机使用多少比例的特征 | "每次决策参考多少个业务因素"——用部分因素做决策，增加模型多样性 |

| 调大（如 1.0） | 调小（如 0.5） |
|---------------|---------------|
| 每次参考全部特征 | 每次随机选一部分，模型更多样 |

**沃尔玛预设 [0.5, 1.0]**：特征多（促销+天气+节假日等），适合较低采样增加多样性。
**麦当劳预设 [0.7, 1.0]**：特征较少，保持较高比例。

**二问/三问**：Optuna 自动搜索。一般不动。

---

### 8. `bagging_fraction` — 样本采样比例

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 每次训练迭代随机使用多少比例的历史数据 | "每次训练用多少历史数据"——用部分数据防止死记硬背 |

| 调大（如 1.0） | 调小（如 0.5） |
|---------------|---------------|
| 用全部历史数据 | 随机采样部分数据，泛化更好 |

**沃尔玛预设 [0.5, 1.0]**：数据充足，适中采样提高泛化。
**麦当劳预设 [0.7, 1.0]**：SKU 少，需充分利用数据。

**二问/三问**：一般不动。数据量特别大时可降低下限。

---

### 9. `extra_trees` — 极度随机树

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 使用极度随机树算法增加随机性 | "要不要增加更多随机性来抵抗异常事件" |

| True | False |
|------|-------|
| 更适合有极端促销的场景 | 更适合稳定需求 |

**沃尔玛**：Optuna 自动选择 True/False。
**麦当劳**：固定 False，需求稳定不需要额外随机性。

**二问/三问**：一般不动。

---

### 10. `objective` — 内部损失函数

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| LightGBM 内部优化的损失函数 | "模型更关注平均值还是极端值" |

| l1 (MAE) | l2 (MSE) |
|----------|----------|
| 关注需求的中位水平，不被极端值影响 | 对极端促销更敏感，关注大单 |

**沃尔玛**：Optuna 自动选择 l1/l2。
**麦当劳**：固定 l1，需求稳定。

**二问**：

| 部门 | l1 | l2 |
|------|----|----|
| 财务 | 库存资金占用更稳定 | 可能因极端值导致安全库存偏高 |
| 销售 | 促销季可能预测偏保守 | 促销季预测更积极 |

**三问**：若促销季缺货频发 → 尝试 l2。若日常库存偏高 → 使用 l1。

---
"""

FULL_DOCUMENTATION = f"""
# AutoMLForecast 框架超参数业务可解释性文档

{LIGHTGBM_PARAMS}

---
> **文档版本**：0.2.5
> **生成日期**：2026-06-05
> **适用框架**：AIDDForecast
> **设计原则**：从数据开始，由判断结束。每个参数都能回答"它对我的库存、资金、缺货风险意味着什么"。
"""

if __name__ == "__main__":
    print(FULL_DOCUMENTATION)