# neuralforecast_hyperparameters_explained.py
"""
NeuralForecast 框架超参数业务可解释性文档
==========================================

本文档对框架内三款正在使用的模型（AutoNHITS, AutoTFT, AutoNBEATx）
的全部内置超参数进行业务层面的翻译解析。

面向读者：
    - 供应链计划部门 / 框架用户：通过"一问"理解每个参数在业务上控制什么
    - 财务 / 销售 / 营销部门：通过"二问"理解参数对自己的影响
    - 技术人员：通过"三问"掌握调整方法和验证指标

模板结构（三问）：
    一问 — 业务上控制什么？调大/调小对库存、资金、缺货风险的影响
    二问 — 对财务和销售/营销部门分别意味着什么？何时调整？
    三问 — 实际效果验证：效果好如何保持？不好如何调整？

预设值来源：
    - 沃尔玛（零售业标杆）：SKU 海量，促销频繁，需要抵抗偶发大单冲击
    - 麦当劳（餐饮业标杆）：SKU 集中，缺货代价极高，需求模式相对稳定
"""

# ═══════════════════════════════════════════════════════════════════
# 第一部分：三款模型共享的通用训练参数
# ═══════════════════════════════════════════════════════════════════

SHARED_PARAMS = """
## 通用训练参数（三款模型共享）

### 1. `h` — 预测步长（预测未来多少周）

**一问：业务上控制什么？**

| 维度 | 说明 |
|------|------|
| 技术含义 | 模型输出未来多少时间点的预测值 |
| 业务翻译 | "你需要提前多久备货"——预测未来 4 周还是 8 周，决定了计划周期长度 |

| 调大（如 8 周） | 调小（如 4 周） |
|----------------|----------------|
| 更早看到远期需求，提前备货 | 更聚焦近期，预测精度更高 |
| 远期预测不确定性增大 | 无法支撑长周期计划 |

| 对库存的影响 | 对资金的影响 | 对缺货风险的影响 |
|-------------|-------------|----------------|
| 调大：远期安全库存可能偏高 | 调大：资金占用周期拉长 | 调大：提前备货可降低远期货源不足风险 |
| 调小：库存更聚焦近期，周转快 | 调小：资金效率高 | 调小：远期缺货预警缺失 |

**预设值**：与业务计划周期对齐。沃尔玛周度补货建议 `h=4` 或 `8`，麦当劳日度补货建议 `h=7` 或 `14`。取自 `Settings` 中的 `forecasting.h`。

**二问：对财务和销售/营销部门分别意味着什么？**

| 部门 | 影响 | 调整建议 |
|------|------|---------|
| 财务 | h 越长，预测准确度越低，安全库存需越大，资金占用越高 | 不要超过业务必需的计划周期 |
| 销售/营销 | h 需覆盖营销活动的最长提前期，否则无法预警活动期缺货 | 与大促提前期对齐 |

**什么时候该调**：业务计划周期变更时同步调整。日常不动。

**三问：实际效果验证**

| 验证指标 | 好的标准 | 不好时的调整 |
|---------|---------|------------|
| 远期预测误差 | 第 h 周预测 vs 实际偏差在可接受范围 | 误差过大 → 缩短 h 或增加安全库存对冲 |
| 计划周期匹配度 | h 覆盖采购+生产+物流周期 | 不匹配 → 调整到等于采购提前期 + 1 周缓冲 |

---

### 2. `input_size` — 输入窗口大小（用多少周历史预测未来）

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 模型回看多少历史数据作为输入 | "参考多久以前的历史来预判未来"——例如用过去 52 周预测下周 |

| 调大（如 104 周） | 调小（如 26 周） |
|------------------|----------------|
| 捕捉更长期趋势，但可能引入陈旧信息 | 更灵敏，但可能忽略年度规律 |

| 对库存 | 对资金 | 对缺货风险 |
|--------|--------|-----------|
| 调大：更稳定的预测，库存平滑 | 调大：资金占用更可预测 | 调大：年度季节性不易漏 |
| 调小：快速响应变化，但可能波动 | 调小：资金效率略高 | 调小：可能漏掉年度大促模式 |

**预设值**：应至少覆盖一个完整的业务周期（如 52 周）。建议 `input_size = max(2 * h, 52)`。

**二问：对财务/销售影响**
- 财务：平稳预测 → 资金计划更可预期。
- 销售：活动周期被覆盖 → 预测能捕捉年度大促。

**什么时候该调**：季节转换时预测偏差大 → 增大；对近期变化反应迟钝 → 减小。

**三问：效果验证**

| 验证指标 | 好的标准 | 不好时的调整 |
|---------|---------|------------|
| 季节性捕捉准确度 | 年度大促前预测有明显上升 | 未捕捉 → 增大 input_size |
| 近期趋势响应速度 | 突发需求变化 2-3 周内预测跟上 | 响应慢 → 减小 input_size |

---

### 3. `learning_rate` — 学习率

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 模型每次更新参数的步长 | "模型学习新模式的激进程度"——是快速适应还是保守微调 |

| 调大（如 0.01） | 调小（如 0.0001） |
|----------------|------------------|
| 快速学习，但可能过冲、不稳定 | 学习慢，但稳定可靠 |

| 对库存/资金/缺货风险 | 影响 |
|---------------------|------|
| 调大：模型可能对突发大单反应过激 → 库存波动 | 缺货风险降低但库存成本上升 |
| 调小：模型稳定，偶发事件可能反应不足 | 库存平稳但缺货风险略高 |

**预设值**：NeuralForecast 默认 `0.001`，两家企业通用。

**二问：财务/销售**
- 财务：一般不动。若预测频繁大幅波动 → 调小。
- 销售：新品类长期预测偏低 → 可考虑略调大。

**三问：效果验证**

| 验证指标 | 好的标准 | 不好时的调整 |
|---------|---------|------------|
| 预测稳定性 | 相邻周预测值不出现剧烈跳变 | 波动大 → 调小 learning_rate |
| 新品类适应速度 | 新品上线 4-6 周内预测趋于合理 | 持续偏低 → 略调大 |

---

### 4. `max_steps` — 最大训练步数

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 模型最多学习多少步后强制停止 | "最多给模型多少时间来学习"——上限控制 |

| 调大（如 2000） | 调小（如 500） |
|----------------|---------------|
| 让模型充分学习，尤其数据量大时 | 训练更快，但可能学习不充分 |

**预设值**：默认 `1000`，通常配合早停机制不需要改动。

**二问**：一般不动。若早停机制频繁触发且预测不佳，可适当增大。

**三问**：若验证 loss 曲线仍在下降就被截断 → 增大 max_steps。

---

### 5. `num_lr_decays` — 学习率衰减次数

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 训练过程中学习率分几次逐步降低 | "模型从快速学习到精细打磨的切换次数" |

| 调大（如 5） | 调小（如 1） |
|-------------|-------------|
| 更细腻的逐步降温，后期更精细 | 快速降温，训练更快 |

**预设值**：默认 `3`，通用。

**二问/三问**：一般不动。若后期预测精度不够 → 略调大（增加衰减次数让后期更精细）。

---

### 6. `batch_size` — 批量大小

**一问：这个参数在业务上控制什么？调大/调小对库存、资金、缺货风险分别意味着什么？**

| 维度 | 说明 |
|------|------|
| **技术含义** | 每次梯度更新使用多少条样本 |
| **业务翻译** | 模型做一次决策参考的"经验条数"——一次看多少个例子再下结论 |

| 调大（批量大） | 调小（批量小） |
|---------------|---------------|
| 一次看很多例子，梯度稳定，但可能忽略个别特殊模式 | 一次看少量例子，对特殊模式敏感，但梯度不稳定 |
| 训练快，显存占用大 | 训练慢，显存占用小 |

| 对库存的影响 | 对资金的影响 | 对缺货风险的影响 |
|-------------|-------------|----------------|
| 调大：忽略个别极端值，预测更平滑，库存更稳定 | 调大：资金占用更可预测 | 调大：可能漏掉个别极端需求，极少数 SKU 缺货风险上升 |
| 调小：对每个 SKU 的细节更敏感，但波动大 | 调小：库存波动可能略大 | 调小：极端需求不易漏，缺货风险更低 |

**两家统一预设 32 的原因**：
- 32 是行业标准值，平衡了梯度稳定性和训练效率
- 两家数据量都充足，32 能保证每个 batch 覆盖足够的模式多样性

**二问：对财务和销售/营销部门分别意味着什么？什么时候该调？什么时候不该动？**

**对财务部门**：`batch_size` 对业务结果影响相对间接，主要体现在训练效率和预测稳定性上。**一般不调**。

**对销售/营销部门**：

| 场景 | batch_size 建议 |
|------|----------------|
| 正常场景 | **不调整**（32 已是最佳平衡点） |
| 显存不足、训练报错 | 调小到 16 |
| 有大量极端促销 SKU，担心预测漏掉尖峰 | 可尝试调小到 16，配合增大 Patience |

**什么情况下该调整**：

| 现象 | 调整方向 | 理由 |
|------|---------|------|
| GPU 显存溢出，训练失败 | 调小（16） | 减少单次显存占用 |
| 部分极端促销品预测持续偏低 | 可尝试调小 | 让小批量训练对极端值更敏感 |
| 正常稳定运行 | **不动** | 32 已是公认最优平衡点 |

**三问：实际效果验证——效果好如何保持？不好如何调整？**

| 验证指标 | 好的标准 | 不好时的调整 |
|---------|---------|------------|
| 训练能否正常完成 | 无 OOM 报错 | 显存溢出 → 调小 |
| 极端促销品预测偏差 | 偏差在可接受范围 | 持续偏低 → 尝试调小 + 增大 Patience |
| 训练稳定性 | loss 曲线平滑 | 不动 |

---

### 7. `valid_batch_size` — 验证批量大小

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 验证时每批用多少条数据 | "每次考试用多少道题"——不影响学习，只影响评估速度 |

**预设值**：`None`（自动等于 batch_size）。一般不动。

---

### 8. `windows_batch_size` — 窗口批量大小

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 每个训练批次采样多少个时间窗口 | "每次看多少个历史片段"——窗口越多覆盖越全 |

| 调大 | 调小 |
|------|------|
| 覆盖更全，训练更稳定 | 训练更快，显存占用更小 |

**预设值**：默认 `1024`，通用。

**二问/三问**：一般不动。显存不足时调小。

---

### 9. `inference_windows_batch_size` — 推理窗口批量大小

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 预测时每批处理多少窗口 | "出结果时一次算多少"——影响预测速度 |

**预设值**：`-1`（全部），通用。一般不动。

---

### 10. `step_size` — 窗口步长

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 相邻训练窗口之间的间隔 | "采样密度"——步长越小，窗口越多，覆盖越密 |

| 调大（如 2） | 调小（如 1） |
|-------------|-------------|
| 窗口少，训练快 | 窗口多，覆盖密，训练慢 |

**预设值**：默认 `1`，通用。一般不动。

---

### 11. `scaler_type` — 数据缩放方式

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 如何对历史数据进行标准化处理 | "用哪种方式统一衡量不同量级的商品"——让大销量和小销量商品公平对待 |

| 选项 | 业务适用场景 |
|------|-------------|
| `'identity'` | 不缩放，适合所有商品量级相近的场景 |
| `'standard'` | 标准化，适合需求稳定、无极端异常 |
| `'robust'` | 抗异常值，适合有促销大单冲击的场景 |
| `'minmax'` | 缩放到 [0,1]，适合需要严格范围控制的场景 |

**预设值**：沃尔玛 `robust`（促销多），麦当劳 `standard`（需求稳定）。

**二问**：对业务部门透明。财务/销售一般不需要关注此参数。

**三问**：若促销品预测持续偏差 → 尝试切换到 `robust`。

---

### 12. `random_seed` — 随机种子

**业务翻译**："复现开关"——相同种子 + 相同数据 = 完全相同的结果。用于保证结果可复现。

**预设值**：默认 `1`。一般不动。

---

### 13. `drop_last_loader` — 丢弃最后不完整批次

**业务翻译**："要不要扔掉不够一组的零头数据"——不影响业务结果。默认 `False`（不丢弃），不浪费任何数据。

**预设值**：`False`。一般不动。

---

### 14. `alias` — 模型别名

**业务翻译**：给模型起个业务能看懂的名字，如 "华东区促销品模型"。纯标识，不影响训练。

---

### 15. `optimizer` / `optimizer_kwargs` — 优化器

**业务翻译**："模型用什么方法学习"——默认 Adam（自适应学习）。一般不动，除非技术人员有特定优化需求。

**预设值**：`None`（使用默认 Adam）。

---

### 16. `lr_scheduler` / `lr_scheduler_kwargs` — 学习率调度器

**业务翻译**："学习率怎么降温"——默认 StepLR（阶梯式下降）。一般不动。

---

### 17. `dataloader_kwargs` — 数据加载器参数

**业务翻译**："数据怎么喂给模型"——如多线程加载等。不影响预测结果，影响训练速度。

---

### 18. `**trainer_kwargs` — PyTorch Lightning 训练器参数

**业务翻译**：底层训练基础设施参数（如 GPU 数量、精度等）。纯技术参数，业务部门无需关注。

---

### 19. `start_padding_enabled` — 开头补零

**业务翻译**："要不要在历史数据最前面补一段零"——让模型对历史较短的商品也能正常训练。默认 `False`。数据充足时不动。

---

### 20. `training_data_availability_threshold` — 训练窗口数据完整度门槛

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 一个训练窗口至少要有多少比例的有效数据才被使用 | "数据多残缺的窗口还值得参考" |

| 调大（如 0.8） | 调小（如 0.0） |
|---------------|---------------|
| 只用数据完整的窗口，质量高但样本少 | 更多窗口参与训练，但可能包含低质量数据 |

**预设值**：默认 `0.0`（不筛选）。数据质量高时不动。若存在大量缺失数据 → 调大到 0.5 以上。

---

### 21. `val_monitor` — 早停监控指标

**业务翻译**："用哪个信号判断模型是不是学够了"——默认监控验证 loss。一般不动。

---

### 22. `early_stop_patience_steps` — 早停耐心步数

**一问：这个参数在业务上控制什么？调大/调小对库存、资金、缺货风险分别意味着什么？**

| 维度 | 说明 |
|------|------|
| **技术含义** | 验证 loss 连续不下降多少个验证周期后，训练自动停止 |
| **业务翻译** | 模型"自我怀疑"的门槛——连续多少次验证没进步，就认为"我学够了" |

| 调大（更耐心） | 调小（更不耐心的） |
|----------------|-------------------|
| 模型多学一会儿，可能捕捉到更隐蔽的需求模式 | 模型尽早收手，避免在噪音上浪费算力 |
| 对促销、季节性等偶发模式更敏感 | 可能漏掉弱信号，但不会过拟合 |
| 训练时间长，算力成本略增 | 训练快，模型轻量 |

| 对库存的影响 | 对资金的影响 | 对缺货风险的影响 |
|-------------|-------------|----------------|
| 调大：预测更保守（捕捉到更多波动），安全库存可能偏高，库存水平上升 | 调大：库存占用资金增加 | 调大：缺货风险降低（考虑了更多波动） |
| 调小：模型更快收敛，预测更平滑，库存水平适中 | 调小：资金占用更少 | 调小：极端波动可能被忽略，缺货风险略微上升 |

**沃尔玛预设 5，麦当劳预设 3 的原因**：
- 沃尔玛有节日促销带来的偶发大单，模型需要更多耐心去"确认"这些不是噪音，而是真实模式
- 麦当劳需求模式更稳定，3 次验证无进步基本说明模型已收敛

**二问：对财务和销售/营销部门分别意味着什么？什么时候该调？什么时候不该动？**

**对财务部门**：

| 调大 Patience | 调小 Patience |
|--------------|--------------|
| 库存资金占用可能增加（安全库存更高） | 资金效率更高 |
| 但如果因此避免了缺货，损失的资金远小于缺货损失的毛利 | 但如果因此漏掉了促销模式，旺季缺货损失可能远超省下的库存成本 |

**对销售/营销部门**：

| 场景 | Patience 建议 |
|------|-------------|
| 营销计划了大促活动，预测需要敏感捕捉 | 调大（让模型多学一会儿，不提前放弃） |
| 日常平稳销售，无特殊活动 | 保持默认或调小 |
| 刚经历过一次异常事件（如自然灾害抢购），不希望模型被它带偏 | 调小（早停，避免过拟合异常点） |

**什么情况下该调整**：

| 现象 | 调整方向 | 理由 |
|------|---------|------|
| 连续两个促销季预测偏低，缺货频发 | 调大 Patience | 模型可能提前停止了，没学到促销模式 |
| 预测曲线频繁波动，库存忽高忽低 | 调小 Patience | 模型可能过拟合了噪音 |
| 正常销售季预测准确，无需调整 | **不动** | 参数已适应当前业务模式 |

**三问：实际效果验证——效果好如何保持？不好如何调整？**

| 验证指标 | 好的标准 | 不好时的调整 |
|---------|---------|------------|
| 促销季预测偏差率 | 偏差 < 10% | 偏差大 → Patience +2 |
| 日常季库存周转天数 | 符合公司基准 | 库存过高 → Patience -1 |
| 缺货率 | 低于目标值 | 缺货高且预测偏低的商品 → Patience +1 |

**向上调整（+1~+2）的业务信号**：销售反馈"促销期经常断货"，且检查预测值确实偏低。
**向下调整（-1~-2）的业务信号**：财务反馈"库存资金占用上升"，且检查预测曲线在平稳期也有波动。
**保持不动的业务信号**：库存周转天数和缺货率都在目标范围内。

---

### 23. `val_check_steps` — 验证间隔步数

**一问：这个参数在业务上控制什么？调大/调小对库存、资金、缺货风险分别意味着什么？**

| 维度 | 说明 |
|------|------|
| **技术含义** | 每训练多少步，做一次验证，检查模型是否进步 |
| **业务翻译** | 模型"复盘"的频率——多久回头看一眼自己学得对不对 |

| 调大（验证间隔长） | 调小（验证间隔短） |
|-------------------|-------------------|
| 训练速度快，但可能在错误方向多跑了一段才回头 | 频繁回头检查，训练慢但不容易跑偏 |
| 适合数据量大、模式稳定的场景 | 适合数据量小、需求快速变化的场景 |

| 对库存的影响 | 对资金的影响 | 对缺货风险的影响 |
|-------------|-------------|----------------|
| 调大：模型可能未及时发现方向错误，预测偏差略大，库存偏差也大 | 调大：轻微增加资金占用不确定性 | 调大：轻微增加缺货不确定性 |
| 调小：模型及时纠偏，预测更准确，库存更精准 | 调小：资金效率更高 | 调小：缺货风险更低 |

**沃尔玛预设 50，麦当劳预设 30 的原因**：
- 沃尔玛 SKU 海量，50 步验证一次平衡了训练效率和模型质量
- 麦当劳 SKU 少但每个都很重要，30 步一次更频繁地确保模型不跑偏

**二问：对财务和销售/营销部门分别意味着什么？什么时候该调？什么时候不该动？**

**对财务部门**：

| 调大 | 调小 |
|------|------|
| 训练更快，算力成本略低 | 训练更慢，但预测更精准 → 库存资金占用更可预测 |
| 偏差风险略高，需要更多安全库存对冲 | 偏差风险低，安全库存可以更精准 |

**对销售/营销部门**：

| 场景 | val_check_steps 建议 |
|------|---------------------|
| 新品类上市，需求模式不明确 | 调小（频繁检查，快速纠偏） |
| 成熟品类，需求模式稳定 | 保持默认或略调大 |
| 全品类批量重训，时间窗口紧张 | 调大（快训快部署） |

**什么情况下该调整**：

| 现象 | 调整方向 | 理由 |
|------|---------|------|
| 训练耗时过长，影响计划产出节奏 | 调大 | 减少验证次数，加速训练 |
| 新上线品类预测偏差大，模型好像"没学好" | 调小 | 增加验证频率，确保模型及时纠偏 |

**三问：实际效果验证——效果好如何保持？不好如何调整？**

| 验证指标 | 好的标准 | 不好时的调整 |
|---------|---------|------------|
| 训练总时长 | 在计划节奏窗口内完成 | 超时 → 调大 val_check_steps |
| 验证 loss 曲线 | 平稳下降，无剧烈震荡 | 震荡大 → 调小，配合增大 Patience |
| 预测准确率 | 符合目标 | 偏差大且 loss 震荡 → 调小 |

---

### 24. `loss` — 训练损失函数
### 25. `valid_loss` — 验证损失函数

（由框架 LOSS_MAP 和路由管道动态选择，不在 settings 中预设。业务含义见 LOSS_MAP 注释。）

**预设值**：`valid_loss` 默认 `None`（自动使用 `loss`）。

---

### 26. `exclude_insample_y` — 排除自回归特征

（仅 NHITS/NBEATx）

**业务翻译**："要不要忽略最近几周的历史值"——默认 `False`（使用最近历史）。若希望模型完全依赖外部特征（如促销标记）而非历史惯性 → 设为 `True`。一般不动。

---

### 27. `futr_exog_list` / `hist_exog_list` / `stat_exog_list` — 外生变量列表

**业务翻译**：
- `futr_exog_list`："哪些未来已知信息会影响需求"——如已排期的促销计划、节假日
- `hist_exog_list`："哪些历史信息会影响需求"——如过去的促销、天气
- `stat_exog_list`："哪些不随时间变化的特征会影响需求"——如门店面积、品类

**业务价值**：这是与业务部门对话的桥梁。销售部门提供促销排期，供应链将其配置到 `futr_exog_list` 中。

**预设值**：从 `Settings` 中的 `exog_config` 读取。不建议硬编码。
"""

# ═══════════════════════════════════════════════════════════════════
# 第二部分：AutoNHITS 专属参数
# ═══════════════════════════════════════════════════════════════════

AUTONHITS_PARAMS = """
## AutoNHITS 专属参数

### `stack_types` — 预测分工方式

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 不同"专家模块"分别负责不同类型的需求模式 | "预测团队怎么分工"——有人看趋势，有人看季节，有人看突发 |

默认 `["identity", "identity", "identity"]` 表示三个通用模块。可选：`"seasonality"`, `"trend"`, `"identity"`, `"exogenous"`。

| 配置 | 业务适用场景 |
|------|-------------|
| 包含 `"seasonality"` | 有明显季节性规律的商品 |
| 包含 `"trend"` | 有长期上升/下降趋势的商品 |
| 包含 `"exogenous"` | 需求受外部因素（促销、天气）影响大的商品 |

**预设值**：保持默认 `["identity", "identity", "identity"]`。技术人员可根据商品特征调整。

**二问/三问**：一般不动。若某品类季节性明显但预测未捕捉 → 增加 `"seasonality"` 模块。

---

### `n_blocks` — 每个模块的专家数量

**业务翻译**："每个分工方向配几个专家"——默认 `[1, 1, 1]` 每个模块 1 个。增大可增强该模块的学习能力，但训练变慢。

**预设值**：保持默认。若某类模式特别复杂（如促销叠加季节性）→ 可增加到 2。

---

### `mlp_units` — 每个专家的深度和宽度

**业务翻译**："每个专家的脑容量"——`[[512, 512]]` 表示两层各 512 个神经元。越大越聪明但越慢。

**预设值**：默认 `3 * [[512, 512]]`。一般不动。

---

### `n_pool_kernel_size` — 池化核大小

**业务翻译**："如何压缩历史信息"——类似"用多大的压缩比例看历史"。默认 `[2, 2, 1]`。一般不动。

---

### `n_freq_downsample` — 频率下采样倍数

**业务翻译**："如何看待不同时间粒度的规律"——`[4, 2, 1]` 分别捕捉月度、双周、周度模式。一般不动。

---

### `pooling_mode` — 池化方式

**业务翻译**："压缩信息的方式"——`"MaxPool1d"` 取最大值保留最显著信号。一般不动。

---

### `interpolation_mode` — 插值方式

**业务翻译**："如何恢复被压缩的信息"——`"linear"` 线性插值。一般不动。

---

### `dropout_prob_theta` — 基础模块的随机丢弃率

**业务翻译**："防死记硬背的力度"——随机忽略一部分信息，提高泛化能力。默认 `0.0`（不丢弃）。若训练数据有限 → 可设为 0.1。

---

### `activation` — 激活函数

**业务翻译**："专家的决策风格"——`"ReLU"` 直接果断，适合多数场景。其他选项如 `"Softplus"` 更平滑。一般不动。
"""

# ═══════════════════════════════════════════════════════════════════
# 第三部分：AutoTFT 专属参数
# ═══════════════════════════════════════════════════════════════════

AUTOTFT_PARAMS = """
## AutoTFT 专属参数

### `hidden_size` — 隐藏层大小

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 模型内部表示的维度 | "模型思考的复杂度"——越大越能捕捉复杂模式 |

| 调大（如 256） | 调小（如 64） |
|---------------|--------------|
| 能捕捉促销叠加季节性等复杂模式 | 训练快，适合简单模式 |

**预设值**：默认 `128`，通用。若商品需求模式复杂（促销+季节+趋势叠加）→ 可增加到 256。

---

### `n_head` — 注意力头数

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 注意力机制中并行的"视角"数量 | "同时从几个角度分析数据"——类似多组专家同时评估 |

| 调大（如 8） | 调小（如 2） |
|-------------|-------------|
| 多角度分析，适合复杂模式 | 训练快，适合简单模式 |

**预设值**：默认 `4`，通用。

---

### `attn_dropout` — 注意力层随机丢弃率

**业务翻译**："注意力机制的防过拟合力度"。默认 `0.0`。若训练数据有限 → 可设为 0.1。

---

### `grn_activation` — 门控残差网络激活函数

**业务翻译**：控制信息流动的"阀门"——`"ELU"` 平滑处理负值。一般不动。

---

### `n_rnn_layers` — RNN 层数

**业务翻译**："记忆链条长度"——处理长期依赖关系。默认 `1`。若需要捕捉超长期模式（如年度周期）→ 可增加到 2。

---

### `rnn_type` — RNN 类型

**业务翻译**："记忆方式"——`"lstm"`（长短期记忆）是最成熟的选择。一般不动。

---

### `one_rnn_initial_state` — 统一初始状态

**业务翻译**："所有序列从同样的记忆起点开始"——默认 `False`（每条序列独立初始化）。一般不动。

---

### `dropout` — 全局随机丢弃率

**业务翻译**："防死记硬背的总力度"——默认 `0.1`（10% 的信息随机忽略）。训练数据量充足可保持，数据少可适当增大到 0.2。

---

### `tgt_size` — 目标变量维度

**业务翻译**："每次预测几个数值"——默认 `1`（只预测需求量）。一般不动。
"""

# ═══════════════════════════════════════════════════════════════════
# 第四部分：AutoNBEATx 专属参数
# ═══════════════════════════════════════════════════════════════════

AUTONBEATX_PARAMS = """
## AutoNBEATx 专属参数

### `n_harmonics` — 谐波数量

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 季节性模块中使用多少个正弦/余弦波 | "用多少个波来描述季节性规律"——越多越精细 |

| 调大（如 4） | 调小（如 1） |
|-------------|-------------|
| 捕捉复杂季节性模式 | 只捕捉简单季节规律 |

**预设值**：默认 `2`，足以处理年度+月度双重季节。若商品有更复杂的多周期性 → 可增加。

---

### `n_polynomials` — 多项式数量

**一问：业务上控制什么？**

| 技术含义 | 业务翻译 |
|---------|---------|
| 趋势模块中使用几阶多项式 | "用几阶曲线描述趋势"——1 阶是直线，2 阶可以弯曲 |

| 调大（如 3） | 调小（如 1） |
|-------------|-------------|
| 能描述更复杂的趋势曲线 | 只适合直线趋势 |

**预设值**：默认 `2`，适合大多数商品。若商品趋势呈现明显的加速增长/衰减 → 可增加到 3。

---

### 以下参数与 NHITS 共享：

- `stack_types`：预测分工方式（同 AutoNHITS）
- `n_blocks`：每个模块的专家数量（同 AutoNHITS）
- `mlp_units`：每个专家的深度和宽度（同 AutoNHITS）
- `dropout_prob_theta`：基础模块的随机丢弃率（同 AutoNHITS）
- `activation`：激活函数（同 AutoNHITS）
- `exclude_insample_y`：是否排除自回归特征（同 AutoNHITS）
"""

# ═══════════════════════════════════════════════════════════════════
# 完整文档合并
# ═══════════════════════════════════════════════════════════════════

FULL_DOCUMENTATION = f"""
# NeuralForecast 框架超参数业务可解释性文档

{SHARED_PARAMS}

---

{AUTONHITS_PARAMS}

---

{AUTOTFT_PARAMS}

---

{AUTONBEATX_PARAMS}

---

> **文档版本**：0.2.5
> **生成日期**：2026-06-03
> **适用框架**：AIDDForecast
> **设计原则**：从数据开始，由判断结束。每个参数都能回答"它对我的库存、资金、缺货风险意味着什么"。
"""

if __name__ == "__main__":
    print(FULL_DOCUMENTATION)