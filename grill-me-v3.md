# grill-me-v3

## 概述

grill-me-v3 是一个基于原版grill-me专项改进后的结构化设计讨论引擎，用于在构建式讨论中引导设计者
从模糊的想法逐步收敛到完整、清晰、可执行的设计方案。

本引擎融入 XGBoost 分支修剪原理（预剪枝 + max_depth + 后剪枝精神）、
德尔菲专家法（多决策者协作扩展），适用于从零构建系统/模块、
多维度决策同时收敛、需设计决策可追溯的场景。

---

## 核心规则

### 1. 聚焦约束

禁止提问与设计构建方案无关的问题；禁止无限制发散性提问。
所有问题必须高质量、有条理、与设计主题高度相关。

若用户提问偏离当前设计树层级，先判断是否属于更高层级未决的遗留问题——
若是则回溯到对应层级补充修剪；若完全无关则提示当前焦点并询问是否暂缓处理。

### 2. 逐层收敛

随着提问轮次增加，对设计方案的约束条件应不断增加，
提问范围应逐渐收窄，设计方案应逐渐清晰直至达成一致。

### 3. 一次性覆盖全层 + 子层拆分

每轮提问必须一次性覆盖当前设计树层的全部分支节点。
一个问题对应一个分支，有多少个分支就提多少个问题。
禁止每轮/每个节点只提问一项问题，禁止遗漏当前层的任何分支节点。

若某一层分支超过8个，将层拆分为两个子层（Layer N-a 和 Layer N-b），
每子层保持4~6个问题。拆分时按分支间依赖关系分组，耦合度高的分支放在同一子层。

### 4. 层修剪机制（融入 XGBoost 预剪枝 + max_depth 思想）

#### 核心机制

- 将设计树的全部分支按有限的层数划分，每层包含多个分支。
  （对应 XGBoost max_depth：预设树的最大深度，结构约束，防止无限生长。）

- 每轮提问对应设计树的其中一层。

- 该层内有多少个分支，就提多少个问题（一个问题对应一个分支）。

- 每轮回答是对该层每个分支的独立判定——是否继续对该分支向下延展。
  （对应 XGBoost γ 阈值精神：不是所有"可分裂"都值得继续。
   判定主体为人类用户，而非数学公式——
   因为设计决策涉及业务判断，无法用增益公式量化。）

- 每轮结果是上一层所有判定为"继续延展"的分支集合。
  这些结果延伸到下一轮，成为下一层的分支问题来源。

- 下一轮仅基于上一轮保留的分支继续向下展开，
  不再回溯已终止延展的分支。
  （对应 XGBoost 预剪枝特点：一旦判定不分裂，该节点即为叶子节点，
   不再对其子节点进行任何计算。）

#### 可选增强机制

- **终层复核**（可选，对应 XGBoost 后剪枝精神）：
  全部层走完后，可基于累积的层决策日志进行一次快速复核，
  检查是否存在明显误判——
  例如某层错误地终止了关键分支，或某层错误地保留了应终止的分支。
  复核不是重新讨论，而是对照最终方案反向校验决策链的一致性。
  若发现误判，回溯到对应层级重新修剪该分支。

- **可恢复标记**（可选，对应 XGBoost γ 参数可调优精神）：
  层决策日志中，对"终止延展"的分支增加可恢复性标记：
  · "永久终止"（默认）：当前阶段确定不再考虑。
  · "暂缓"：当前阶段优先级低，但后续迭代可能重新纳入。
  标记为"暂缓"的分支在未来迭代中可被唤醒，
  无需重新走完整设计树，直接从该分支所在层切入继续延展。

### 5. 已有资源优先

如果某个问题可以通过探索以下已有资源来回答，优先探索已有资源而非提问：
- 代码库（项目源代码）
- 已有文档（SKILL.md、README.md、参考文件）
- 配置文件（config/、data/目录下的JSON文件）
- 参考数据（references/目录下的模板和案例文件）

已有资源是唯一的事实来源，优于主观判断。

### 6. 推荐答案 + 多方案

对于每个问题，必须提供推荐答案供用户参考。
推荐答案应基于框架设计哲学和已有原则体系推导，降低用户的决策负担。

当决策点存在多个合理方案且存在显著取舍时，
提供"推荐方案 + 备选方案"双选项，各附适用场景说明。

### 7. 层决策日志

每轮提问结束后，在进入下一轮之前，输出一份简短的决策记录，包含：
- 本轮层号与主题
- 本轮新增约束条件清单
- 本轮判定为"继续延展"的分支列表
- 本轮判定为"终止延展"的分支列表及终止原因（供后续迭代回溯）

决策日志随设计树推进逐层累积，形成完整的设计决策链。

---

## 扩展模块

### v3.2 — 多决策者协作模式（基于德尔菲专家法）

#### 适用条件

当设计树某一层的分支判定由多个 AI Agent 共同参与时启用。
每个 Agent 被赋予特定角色视角（如成本专家、合规专家、技术专家），
从各自视角对每个分支进行独立判定。

#### 层内德尔菲循环（每层执行一轮，替代原"用户单一判定"）

**Step 1 — 独立评估 (Independent Assessment)**

协调人（grill-me 引擎）将本层所有分支问题同时发给所有参与 Agent。
每个 Agent 独立对每个分支做出判定：
- "继续延展" + 理由摘要（基于该 Agent 的角色视角）
- "终止延展" + 理由摘要

Agent 之间互不知晓其他 Agent 的判断。
（对应德尔菲：第一轮开放探索 + 匿名性）

**Step 2 — 统计汇总与反馈 (Aggregation & Feedback)**

协调人汇总所有 Agent 的判断，计算每个分支的：
- 投票分布：继续延展 N₁ 票 / 终止延展 N₂ 票
- 分歧度指标：
  · 高共识：≥2/3 票倾向同一方向
  · 中分歧：票数差距 ≤1
  · 高分歧：各方向均有票但无绝对多数
- 匿名理由摘要：将各方理由（不标注来源Agent）按"继续派"和"终止派"分类整理

协调人将此汇总结果反馈给所有 Agent。
（对应德尔菲：统计汇总 + 受控反馈）

**Step 3 — 修订评估 (Revision)**

每个 Agent 看到群体判断分布和匿名理由摘要后，
重新审视自己的判定。可以：
- 维持原判，补充反驳理由
- 修改判定（被对方理由说服）

Agent 提交修订后的判定。
（对应德尔菲：第二/三轮修订收敛）

**Step 4 — 共识判定与分歧升级 (Consensus & Escalation)**

协调人根据修订后的投票分布做出最终判定：

- 高共识达成 → 采纳多数方向（延展或终止），记录决策。
- 中分歧 → 取多数方向，但标注"存在分歧，后续迭代复核"。
- 高分歧且经修订后仍未收敛 → 触发分歧升级规则：
  - 若用户（人类决策者）可用 → 提交用户做最终裁定。
  - 若纯AI协作 → 采用预设的"安全侧原则"——
    例如：涉及合规/安全的分支，分歧时默认"保留延展"（宁可多讨论）；
    涉及效率/优化的分支，分歧时默认"终止延展"（不做过度设计）。

**Step 5 — 输出层决策日志（M7 扩展）**

在层决策日志中增加：
- 参与 Agent 列表及各自角色
- 每个分支的投票分布演变（Step 1 → Step 3）
- 最终判定结论 + 分歧度标记
- 若有分歧升级，记录升级原因和最终裁定主体

---

## 适用场景

| 适用条件 | 说明 |
|----------|------|
| 从零构建新系统/新模块 | 设计树从第1层（核心边界）开始，逐层向下展开 |
| 多个决策维度需要同时确定 | 每层多个分支同时修剪，避免"A决定完了发现与B冲突"的返工 |
| 设计者已有初步想法但需要结构化梳理 | M6推荐答案机制让设计者从"想清楚再回答"变为"判断+微调" |
| 单次讨论时间有限，需要高效收敛 | M3一次性全层提问+M4逐层修剪，控制讨论轮次 |
| 需要设计决策可追溯 | M7层决策日志提供完整追溯链，供后续迭代回溯 |

## 不适用场景

| 场景 | 原因 | 替代建议 |
|------|------|----------|
| 纯探索性头脑风暴（还不知道要构建什么） | 层修剪机制会过早排除可能性 | 先用自由讨论模式发散，确定方向后再切换到 v3 |
| 简单/单维度决策 | 设计树层级机制过于重型 | 直接提问即可 |
| 已有成熟方案，仅需微调 | 从零设计假设会导致过度讨论 | 使用 v3.1 已确认层注入（规划中） |

## 未来迭代方向

| 版本 | 内容 | 状态 |
|------|------|------|
| v3.1 | 已确认层注入——允许用户在启动时声明已有决策层，从第N+1层开始修剪 | 规划中 |
| v3.2 | 多决策者协作模式（德尔菲嵌入版）——层内德尔菲循环5步骤 | ✅ 已写入 |
| v3.3 | 自动检测遗漏分支——AI主动扫描每层可能涉及的决策维度并提示遗漏 | 规划中 |
| v3.4 | 决策日志→设计文档自动生成——基于M7日志自动生成结构化A-3方案确认文档 | 规划中 |

---

## 版本历史

| 版本 | 日期 | 变更摘要 |
|------|------|----------|
| v1 | — | 原始 grill-me：每次只问一个问题，无结构约束 |
| v2 | 2026-07-26 | 新增6条核心规则（聚焦约束/逐层收敛/一次性全层/层修剪/代码库优先/推荐答案） |
| v3 | 2026-07-28 | M1扩展偏离处理；M3扩展子层拆分；M4修正精准定义+终层复核+可恢复标记；M5扩展已有资源优先；M6扩展多方案；新增M7层决策日志；新增v3.2德尔菲嵌入扩展模块 |